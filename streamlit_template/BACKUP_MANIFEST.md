813:def show_login_page():
833:def show_logout_button():
1598:def show_omset_trend_table(df_filtered: pd.DataFrame, df_full: pd.DataFrame, config: Config, current_period: Optional[str], months: int = 12):
1811:def show_main_dashboard(df, config, processor, viz, current_period, compare_period, full_df):
1879:def show_outlet_crud(df, config, processor):
2088:def show_outlet_crud_v2(df, config, processor):
2758:def show_ai_decision_center(df: pd.DataFrame, config: Config):
2799:def show_trend_analysis_v2(df, config, processor, viz):
3053:def show_trend_analysis(df, config, processor, viz):
3064:def show_conversion_analysis(df, config, processor, viz):
3122:def show_outlet_ranking(df, config, processor):
3159:def show_period_comparison(df, config, processor, viz, current_period, compare_period):
3176:def show_user_access_panel():
3259:def show_monthly_database_panel(config: Config):
3355:def show_admin_panel(config):
3476:def show_kemitraan_page(df: pd.DataFrame, config: Config, processor: DataProcessor):
3813:def show_upload_data(config: Config):
